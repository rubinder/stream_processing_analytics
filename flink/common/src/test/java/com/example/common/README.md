# Tests for com.example.common

`SectorLookupTest` — checks that `sectorFor` resolves a known symbol (e.g., `AAPL` → `Technology`), returns `UNKNOWN` for unknown symbols, and `isKnown` matches.

Run: `mvn -pl common test`.
